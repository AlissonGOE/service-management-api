info:
  name: List all clients
  type: http
  seq: 1

http:
  method: GET
  url: {{baseUrl}}/clients
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
