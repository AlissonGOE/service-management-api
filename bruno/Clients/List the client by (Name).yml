info:
  name: List the client by (Name)
  type: http
  seq: 2

http:
  method: GET
  url: {{baseUrl}}/clients/name/{{name}}
  auth: inherit

runtime:
  variables:
    - name: name
      value: String

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
