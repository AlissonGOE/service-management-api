info:
  name: List the service order by (Id)
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/service-orders/{{id}}
  auth: inherit

runtime:
  variables:
    - name: id
      value: "65"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
